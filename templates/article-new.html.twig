<!DOCTYPE html>
<html lang="fr">
<head>
    <link rel="stylesheet" href="../css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajout Articles</title>
</head>

<body id="fond">

    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="#">
            <img src="https://wewebstyle.com/img/logos/logos.png" width="40" height="30" alt="" loading="lazy">
        </a>
    </nav>

    <div class="container">

        <form method="POST" class="marge">
            <div class="form-group row">
              <label for="name" class="text-center col-12 col-form-label">Nom</label> 
              <div class="col-8 center">
                <input id="name" name="name" placeholder="Nom de L'article" type="text" class="form-control {%  if errors.name is defined %} is-invalid {% endif %}" value="{{ formData.name }}">
                {%  if errors.name is defined %}
                <div class="invalid-feedback">
                    {{ messages.name }}
                </div>
                {% endif %}
            </div>
            </div>
            <div class="form-group row">
              <label for="description" class="text-center col-12 col-form-label">Description</label> 
              <div class="col-8 center">
                <input id="description" name="description" placeholder="Description de L'article" type="text" class="form-control {%  if errors.description is defined %} is-invalid {% endif %}" value="{{ formData.description }}">
                {%  if errors.description is defined %}
                    <div class="invalid-feedback">
                        {{ messages.description }}
                    </div>
                {% endif %}
            </div>
            </div>
            <div class="form-group row">
              <label for="price" class="text-center col-12 col-form-label">Prix</label> 
              <div class="col-8 center">
                <input id="price" name="price" placeholder="Prix de L'article" type="text" class="form-control {%  if errors.price is defined %} is-invalid {% endif %}" value="{{ formData.price }}">
                {%  if errors.price is defined %}
                    <div class="invalid-feedback">
                        {{ messages.price }}
                    </div>
                {% endif %}
              </div>
            </div>
            <div class="form-group row">
              <label for="quantity" class="text-center col-12 col-form-label">Quantité</label> 
              <div class="col-8 center">
                <input id="quantity" name="quantity" placeholder="Quantité" type="text" class="form-control {%  if errors.quantity is defined %} is-invalid {% endif %}" value="{{ formData.quantity }}">
                {%  if errors.quantity is defined %}
                    <div class="invalid-feedback">
                        {{ messages.quantity }}
                    </div>
                {% endif %}
              </div>
            </div> 
            <div class="form-group row">
              <div class="offset-0 col-8 center">
                <button name="submit" type="submit" class="center btn btn-primary" style="display: flex;">Submit</button>
              </div>
            </div>
          </form>

          <a href="../public/articles.php">
            <button class="btn btn-primary" style="display: flex;">Articles</button>
            </a>

    </div>


</body>
</html>